event_inherited();

impact  = false;
explode = false;

dir = 404;

image_speed = 0;
phy_fixed_rotation = true;
image_speed = 0;
alarm[0] = room_speed * 1.5; //Fuse

event_perform(ev_alarm,1); //bomb delay currently "NULL"

scr_pausable_init(3);
