//this list holds all the alive mages in the room
mages = ds_list_create();

//this list holds all the fires
fires = ds_list_create();

//holds the pairs
pairs = ds_list_create();

//variable that determines if there need to be calculations of pairing
calculate = false;

